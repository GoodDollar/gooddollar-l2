import HedgeProofViewerLatestClient from './HedgeProofViewerLatestClient'

export const dynamic = 'force-dynamic'

export default function HedgeProofViewerLatestPage(props: any) {
  return <HedgeProofViewerLatestClient {...props} />
}
