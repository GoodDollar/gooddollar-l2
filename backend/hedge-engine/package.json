{
  "name": "@goodchain/hedge-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Off-chain hedge engine — maps GoodChain exposure to eToro hedge orders",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch",
    "clean": "rm -rf dist",
    "hedge:demo": "ts-node src/cli-demo.ts"
  },
  "dependencies": {
    "@goodchain/etoro-client": "file:../etoro-client",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  }
}
